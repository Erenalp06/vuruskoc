Put screenshots here (referenced from the top-level README):

  docs/result.png   — the analysis result screen
  docs/trim.png      — the upload + trim step (optional)

PNG, ~1400px wide is plenty. `docs/*.png` is committed (not gitignored).
